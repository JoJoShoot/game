请用python写一个可执行的代码，要求如下：
1、连接数据库，数据库类型为monggodb，数据库名game，数据表名game；
2、按数据表中tags值为4200执行分页查询操作，每页1000行；
3、将查询结果中的mobile列写入txt文件中，一行一条，每写入一次提示一次“成功+1”；
4、pymongo版本为4.3.3；
5、请避免使用count或者count_documents方法，避免使用while循环；


请用python写一个项目，要求如下：
1、连接数据库，数据库类型为monggodb，数据库名game，数据表名game；
2、查询表中所有的tags值，并以复选框的形式，横向排列展示在前端页面中，只要保证横向展示，其他可以尽量精简;
3、在前端页面中，可以选择一个或多个tags值，依据选择的tags值，点击查询按钮进行数据总量查询，并显示查询结果数量；
4、提供数据下载功能，点击下载按钮后，以当前选定的tags值为查询条件，将查询结果中的mobile项以txt文件导出并下载，下载时一行一条记录；
5、导出时请使用分页查询以提高稳定性；
6、导出文件时，务必参考这段代码作为导出代码：
# 分页查询并将查询结果中的 mobile 列写入文本文件中
with open("9845.txt", "w") as f:
    for result in collection.find(query, {"mobile": 1}).batch_size(10000):
        mobile = result['mobile']
        f.write(mobile + "\n")
        count += 1
        if count % 10000 == 0:
            print(f"成功+{count}")；
7、导出的文件名以tags值命名；
